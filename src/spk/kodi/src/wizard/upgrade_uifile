[
  {
    "step_title": "Upgrade Notice",
    "items": [
      {
        "desc": "This package must be run as root, but Synology prohibits this for packages that have not been verified by them."
      },
      {
        "desc": "Please continue the installation, then ssh into DSM or use the task scheduler (root) to execute the following commands:"
      },
      {
        "desc": "<code>sudo sed -i 's/package/root/g' /var/packages/kodi/conf/privilege  # Change permissions to root</code>"
      },
      {
        "desc": "<code>sudo synopkg restart kodi # Restart the service</code>"
      },
      {
        "type": "singleselect",
        "desc": "Network mode",
        "subitems": [
          {
            "key": "wizard_network_host",
            "desc": "host - UPnP/DLNA & AirPlay streaming over the local network",
            "defaultValue": false
          },
          {
            "key": "wizard_network_bridge",
            "desc": "bridge (default) - isolated network with port mapping",
            "defaultValue": true
          }
        ]
      }
    ]
  }
]
